# ── Game files (archives and generated binaries) ─────────────────────────────
# Engine.ucs is tracked (mod content). SGA archives are NOT tracked.
EnginLoc.sga
EnginLoc.sga0
*.sga
*.sga.disabled

# ── Virtual environments ───────────────────────────────────────────────────────
.venv
.venv*/

# ── Python build artifacts ─────────────────────────────────────────────────────
dist/
build/
*.egg-info/
.pytest_cache/
.coverage
__pycache__/
*.pyc
*.pyo

# ── Backups and temp files ─────────────────────────────────────────────────────
*.bak
backup/
*.tmp
*.old
~*

# ── Deploy state (written by deploy scripts at runtime) ──────────────────────
.copilot_workspace/

# ── OS / IDE ──────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Game-specific ─────────────────────────────────────────────────────────────
*.arciv
